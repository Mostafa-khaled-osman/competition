🧑‍🏫 Teachers API – المدرسين
1. عرض كل المدرسين

URL: /api/teachers

Method: GET

Description: بيعرض كل المدرسين في قاعدة البيانات.

Example:
GET http://localhost:5000/api/teachers

2. عرض مدرس واحد بالـ ID

URL: /api/teachers/:id

Method: GET

Example:
GET http://localhost:5000/api/teachers/1

3. إضافة مدرس جديد

URL: /api/teachers

Method: POST

Body:

{
  "name": "Mohamed Ali",
  "subject": "Math",
  "email": "mohamed@example.com"
}


Example:
POST http://localhost:5000/api/teachers

4. تعديل بيانات مدرس

URL: /api/teachers/:id

Method: PUT

Body: نفس فورم الإضافة.

5. حذف مدرس

URL: /api/teachers/:id

Method: DELETE

👨‍🎓 Students API – الطلاب
1. عرض كل الطلاب

URL: /api/students

Method: GET

2. عرض طالب واحد

URL: /api/students/:id

Method: GET

3. إضافة طالب

URL: /api/students

Method: POST

Body:

{
  "name": "Ahmed Hassan",
  "class_id": 1,
  "email": "ahmed@example.com"
}

4. تعديل بيانات طالب

URL: /api/students/:id

Method: PUT

5. حذف طالب

URL: /api/students/:id

Method: DELETE

🏫 Classes API – الفصول

(نفس أسلوب الطلبات: GET كل الفصول، GET فصل بالـ ID، POST إضافة، PUT تعديل، DELETE حذف)
رابط البداية: /api/classes

📚 Subjects API – المواد الدراسية

رابط البداية: /api/subjects

📝 Assignments API – الواجبات

رابط البداية: /api/assignments

📰 News API – الأخبار

رابط البداية: /api/news

⏳ Schedule API – الجداول

رابط البداية: /api/schedule

📋 Attendance API – الحضور والغياب

رابط البداية: /api/attendance

📌 كل الـ POST و PUT لازم ترسل البيانات في JSON Body بالشكل اللي فوق.
📌 كل الـ GET و DELETE ما يحتاجوش Body.